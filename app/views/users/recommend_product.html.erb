<body>
  <br>
  <section class="product-filter-section">
    <div class="container">
      <div class="row">
        <% @recommend_products[0...8].each do |product| %>
          <%= render "products/product", {product: product} %>
        <% end %>
      </div>
    </div>
  </section>
</body>
