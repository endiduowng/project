<% if is_favorited.present? %>
  <%= link_to favorite_path(is_favorited), class: "wishlist-btn", method: :delete, remote: true do %>
    <i class="flaticon-heart" style="color: red;"></i>
  <% end %>
<% else %>
  <%= link_to product_favorites_path(product), class: "wishlist-btn", method: :post, remote: true do %>
    <i class="flaticon-heart"></i>
  <% end %>
<% end %>
